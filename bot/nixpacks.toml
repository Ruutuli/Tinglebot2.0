[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
# Install from repo root where package.json is located
# Railway builds from repo root even when Root Directory is set to bot/
cmds = ["npm install"]

[phases.build]
cmds = []

[start]
cmd = "node bot/index.js"
