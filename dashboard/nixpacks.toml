[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
# Install from repo root where package.json is located
# Railway builds from repo root even when Root Directory is set to dashboard/
cmds = ["npm install"]

[phases.build]
cmds = []

[start]
cmd = "node dashboard/server.js"
